'use strict';

//alert("test");

let myLibrary = [];

function Book(author, title, numPages, status = false) {
    this.author = author;
    this.title = title;
    this.numPages = numPages;
    this.status = status;
}

function addBookToLibrary() {
    // do stuff here
}